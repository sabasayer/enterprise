describe("EnterpriseCollectionProvider", () => {
    it("should call get", () => {});

    it("should call save", () => {});

    it("should call delete", () => {});

    it("should call get when cache is empty and set data to cache", () => {});

    it("should not get data from cache and calls get when forceGetFromApi is true", () => {});

    it("should call get when items from cache is lacking", () => {});

    it("should set data to cache on save", () => {});

    it("should remove data from cache on delete", () => {});
});
